"use client";

/**
 * Direct re-export — the growth story is imported eagerly so HTML always
 * contains the visible section (no ssr:false empty skeleton).
 */
export { WebsiteGrowthSystemScroll as WebsiteGrowthSystemScrollLazy } from "./WebsiteGrowthSystemScroll";
